currently project only support chrome

launch the app:
(1) using webserver for chrome;
(2) select scenario folder;
(3) click the web server url;


play with the app

on the left side :
(1) the maps will show lower layer when click the ploygon;
(2) click the back button will return to upper layer;
(3) drag the split line will show the color comparison of two maps
on the right side:

(1) the tow sides bar chart will show the data comparison of maps
